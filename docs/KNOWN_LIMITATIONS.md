# Известные ограничения (0.1.0-alpha)

Правило продукта: функции, которых нет, не показываются в интерфейсе.
Ни одна кнопка не имитирует работу.

## Ещё не реализовано (по этапам плана)

| Функция | Этап | Примечание |
| --- | --- | --- |
| Изолированный процесс PdfWorker | 3 | сейчас PDFium работает в выделенном потоке основного процесса; сбой нативной библиотеки может уронить приложение |
| Пересжатие изображений (профили качества) | 3 | сейчас есть структурная оптимизация без потерь (qpdf); понижение DPI/JPEG-качества — следующая итерация |
| Редактирование СУЩЕСТВУЮЩЕГО текста/объектов | 4 | PDFium не даёт надёжного редактирования исходных текстовых объектов; добавление нового контента уже работает. Настоящее редактирование — только с продвинутым движком (Advanced-адаптер) |
| Redaction | 4 | |
| Перемещение/удаление уже добавленного оверлея мышью | 4 | пока: Ctrl+Z до сохранения; после сохранения контент запечён |
| Ссылки (создание/правка) | 4 | |
| Ответы/статусы комментариев, штампы, карандаш | 5 | заметки, маркер-выделение, рамка и овал уже работают; удаление СУЩЕСТВУЮЩИХ аннотаций файла — следующая итерация (свои черновики удаляются) |
| Формы: выпадающие списки (combo), мигающий курсор в поле | 5 | текстовые поля, флажки и переключатели заполняются; combo требует собственного popup-UI; каретка не мигает (таймеры pdfium отключены), но позиция ввода работает |
| Формы XFA | 5 | не поддерживаются осознанно (legacy-формат Adobe); кнопка «Формы» не показывается |
| Создание/редактор полей форм | 5 | заполнение работает; конструктор полей — отдельная итерация |
| Визуальная подпись картинкой | 6 | |
| Подпись: видимое поле, TSA-метка времени, несколько подписей, запароленные файлы | 6 | невидимая подпись PKCS#7 и полная проверка (криптография/изменения/доверие) уже работают; повторное подписание уже подписанного файла честно блокируется (конвейер нормализует файл); ручная проверка в Adobe Acrobat не проводилась |
| Смена пароля/владельца существующей защиты | 6 | создание защищённой копии (AES-256) и чтение защищённых файлов уже работают |
| OCR: выбор произвольного диапазона страниц, другие языки кроме rus+eng, автоповорот кривых сканов, редактирование распознанного текста | 7 | распознавание rus+eng с невидимым слоем уже работает; текст доступен после сохранения; страницы с текстом честно пропускаются |
| Получение изображений со сканера (WIA/TWAIN) | 7 | |
| Конвертация форматов, пакетная обработка, CLI-операции | 8 | сейчас CLI принимает только пути файлов для открытия |
| Сравнение документов, PDF/A, shell-расширения, updater | 9 | |
| Перетаскивание страниц между документами/окнами | 2→след. итерация | модель (`PageRef` с чужим источником, `InsertPagesOperation`) уже поддерживает; нет UI-жеста |
| Закладки/оглавление документа | след. итерация | |
| Подпись кода артефактов | установщик | Setup.exe/MSI не подписаны сертификатом — при скачивании из интернета SmartScreen предупредит; локальная установка работает без предупреждений |
| Per-machine установка «для всех» | установщик | реализована (UAC через Setup), но автоматически прогнан только per-user цикл |
| Размер Setup.exe (~117 МБ) | установщик | self-contained рантайм дважды (Setup + приложение в MSI); план — framework-dependent Setup или нативный стуб |

## Технические ограничения текущей реализации

- Файлы больше 2 ГБ не открываются (лимит `FPDF_LoadMemDocument`).
- После сохранения история Undo очищается (документ переоткрывается из
  результата) — задокументированное поведение, не баг.
- Сетка режима «Страницы» не виртуализирована: на документах в тысячи страниц
  переключение в режим систематизации заметно дороже просмотра.
- Смена языка интерфейса применяется после перезапуска.
- Пароль к документу движок принимает в однобайтовой кодировке (латиница —
  надёжно; кириллические пароли зависят от того, чем файл шифровался).
- Тёмная тема не перекрашивает системные скроллбары и стандартные диалоги
  Windows (открытие/сохранение файлов).
- UI-автоматизация не выполнялась; функционально проверены движки (тестами),
  запуск/открытие (смоук по логу) и полный цикл установщика (per-user).
- Печать реализована, но не покрыта автотестами (нет надёжного способа
  печатать в файл без диалога драйвера); проверяйте на реальном принтере.
- Рендер выполняется в DIP-разрешении с учётом системного DPI на момент
  показа страницы; при переносе окна на монитор с другим DPI страница
  дорисуется в новом разрешении только после повторного попадания в
  виртуализацию (прокрутки).
