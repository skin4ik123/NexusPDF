# Известные ограничения (0.17.0-alpha)

Правило продукта: функции, которых нет, не показываются в интерфейсе.
Ни одна кнопка не имитирует работу.

## Ещё не реализовано (по этапам плана)

| Функция | Этап | Примечание |
| --- | --- | --- |
| Изолированный процесс PdfWorker | 3 | сейчас PDFium работает в выделенном потоке основного процесса; сбой нативной библиотеки может уронить приложение |
| Пересжатие: прозрачные (по альфе рендера), 1-битные (трафареты/bilevel), CCITT/JBIG2/JPX, защищённые (включая owner-пароль) | 3→готово в 0.11 | понижение DPI + JPEG работает (UI и CLI compress); перечисленное честно пропускается — их пересжатие портило бы вид или молча снимало защиту; CMYK/ICC-исходники запекаются в sRGB (возможен лёгкий сдвиг цвета); во время сжатия рендер других вкладок ожидает |
| Редактирование СУЩЕСТВУЮЩЕГО текста: перенос строк, изменение шрифта/кегля, правка сразу нескольких строк | 4→готово в 0.16 | правка ОДНОГО текстового объекта работает: щелчок по строке открывает её содержимое, новый текст пишется в тот же объект и сохраняет шрифт, кегль, цвет и место. Ограничения измерены: строка не переносится и не переверстывается (в PDF нет абзацев — каждый объект самостоятелен); у встроенного подмножества шрифта может не быть нужных букв, и это проверяется РИСОВАНИЕМ пробного объекта до сохранения, с предупреждением по мере ввода; отсканированный текст так не правится (сначала OCR) |
| Вымарывание: страница растеризуется (250 DPI; у больших форматов A1/A0 — ниже из-за предела стороны 6000 px) | 4→готово в 0.13 | гарантия уничтожения данных обменена на текстовый слой страницы: вымаранная страница — изображение (текст вернёт OCR); слова OCR-слоя под вымаркой отбрасываются; до сохранения вымарка-черновик НЕ действует на печать и экспорт; массовые вымарки требуют память пропорционально числу вымарываемых страниц |
| Перемещение/удаление уже добавленного оверлея мышью | 4 | пока: Ctrl+Z до сохранения; после сохранения контент запечён |
| Ссылки (создание/правка) | 4 | |
| Ответы/статусы комментариев, штампы | 5 | заметки, маркер-выделение, рамка, овал, карандаш, линия и стрелка уже работают; свои черновики и существующие аннотации файла удаляются из панели комментариев (кроме виджетов форм — они часть AcroForm) |
| Рисование: ластик, изменение нарисованного, нажим пера | 5→частично в 0.16 | карандаш со стабилизацией дрожания, линия и стрелка с привязкой к 45° по Shift, автовыпрямление почти прямых штрихов и возврат свободного штриха работают; нарисованное удаляется через Ctrl+Z до сохранения или из панели комментариев (это Ink-аннотация); давление пера не читается — WPF-события мыши его не несут |
| Формы: мигающий курсор в поле, редактируемые combo (ввод своего текста) | 5 | текстовые поля, флажки, переключатели и выпадающие списки заполняются; каретка не мигает (таймеры pdfium отключены), но позиция ввода работает; в combo выбирается пункт из списка (свободный ввод в editable-combo — следующая итерация) |
| Формы XFA | 5 | не поддерживаются осознанно (legacy-формат Adobe); кнопка «Формы» не показывается |
| Создание/редактор полей форм | 5 | заполнение работает; конструктор полей — отдельная итерация |
| Визуальная подпись картинкой | 6 | |
| Подпись: TSA-метка времени, несколько подписей, запароленные файлы | 6 | невидимая подпись PKCS#7, полная проверка и видимая отметка (запекается в страницу, позиция фиксированная — левый низ первой страницы, текст на русском) уже работают; повторное подписание честно блокируется; ручная проверка в Adobe Acrobat не проводилась |
| Смена пароля/владельца существующей защиты | 6 | создание защищённой копии (AES-256) и чтение защищённых файлов уже работают |
| OCR: выбор произвольного диапазона страниц (только «все» или «текущая»), автоповорот кривых сканов, распознавание таблиц как таблиц | 7 | движки PaddleOCR PP-OCRv6 (16 языковых пакетов) и Tesseract 5 выбираются в окне; невидимый слой и режим редактируемого текста работают сразу, до сохранения; наклон строк модель терпит, но сильно повёрнутый скан лучше выровнять заранее; ячейки таблиц распознаются как обычные строки |
| OCR смешанных страниц (скан + любой настоящий текст: штамп, колонтитул) | 7 | страница с ЛЮБЫМ уже имеющимся текстом честно пропускается — повторное распознавание создало бы невидимый дубль, и копирование возвращало бы слова дважды; вычитание пересечений со существующим текстом — следующая итерация |
| Прерывание распознавания МГНОВЕННО | 7 | отмена срабатывает после завершения текущей страницы (нативные вызовы Tesseract и ONNX Runtime непрерываемы); повторное закрытие окна прогресса отпускает окно, не дожидаясь |
| Получение изображений со сканера (WIA/TWAIN) | 7 | |
| Конвертация: DOCX/HTML/EPUB и другие текстовые форматы | 8 | экспорт в изображения/текст, PDF из изображений, объединение и NexusPdfCli уже работают; офисные форматы требуют стороннего конвертера — отдельная итерация |
| Объединение PDF: закладки, формы AcroForm и вложения исходников | 8 | переносятся страницы и аннотации; остальное теряется (ограничение FPDF_ImportPages) — предупреждение есть в справке CLI |
| Отмена длинного экспорта изображений из интерфейса | 8 | прогресс показывается постранично, но кнопки отмены нет (в CLI — Ctrl+C); гигантские страницы рендерятся с ограничением стороны 8000 px, фактический DPI пишется в файлы честно |
| Пакетная обработка: OCR и произвольные цепочки операций | 8 | пакетные оптимизация/пароль/экспорт PNG уже работают; из CLI пакетный OCR доступен циклом по файлам |
| Сравнение: пословный diff для страниц длиннее 4000 слов и сканов без OCR | 9 | визуальное сравнение с подсветкой и пословный текстовый diff (UI + CLI compare --text) работают; длинные страницы сравниваются только визуально (защита от квадратичного LCS), у сканов без текстового слоя diff пуст; защищённые файлы сравнивает CLI (--password-a/--password-b), в окне — пока нет |
| Сравнение значений полей форм | 9 | сравнивается отрисованное содержимое страниц; значение поля без сгенерированного внешнего вида (appearance stream) может быть не видно рендеру — такие файлы могут показаться «идентичными» |
| «Отправить → NexusPDF» при установке «для всех» | 9 | папка SendTo персональная: ярлык появляется у пользователя, выполнившего установку; при обычной установке «для меня» — всё корректно |
| Конверсия В PDF/A и полная валидация PDF/A | 9 | заявленное соответствие показывается в свойствах; конверсия/валидация требуют стороннего движка (Ghostscript/veraPDF) — отдельная итерация |
| Автообновление | 9 | честно не реализовано: у продукта нет сервера обновлений; проверка версии без сервера была бы фальшивой кнопкой |
| Пункт контекстного меню Проводника | 9 | вместо него «Отправить → NexusPDF» (SendTo): работает на всех версиях Windows, включая Win11 24H2+, где HKCU-пункты меню игнорируются |
| Перетаскивание страниц между документами/окнами | 2→след. итерация | модель (`PageRef` с чужим источником, `InsertPagesOperation`) уже поддерживает; нет UI-жеста |
| Закладки: создание и правка оглавления | след. итерация | чтение и переход работают: дерево с вложенностью, цели из /Dest и из действия /GoTo, пересчёт после перестановки и удаления страниц. Узел с битой целью (в реальных файлах встречается `/Dest [null …]`) честно показывается приглушённым и не притворяется ссылкой |
| Слои (OCG): переключение прямо в открытой вкладке | след. итерация | список слоёв и переключение видимости работают через сохранение копии: у pdfium нет публичного API для слоёв, конфигурация читается и пишется через qpdf, а движок учитывает её при ОТКРЫТИИ файла. Живое переключение потребовало бы переоткрытия документа с потерей истории правок. Содержимое выключенного слоя остаётся в файле — это не вымарывание |
| Вложения: открытие вложенного файла | никогда | список и сохранение на диск работают; запуск вложения не будет сделан осознанно — это классический способ доставки вредоносного файла. Описание вложения не показывается: `FPDFAttachment_GetStringValue` в поставляемой сборке pdfium возвращает пустую строку и для `/Desc`, и для ключей `/Params` (проверено тестом), поэтому поле было бы вечно пустым |
| Подпись кода артефактов | установщик | Setup.exe/MSI не подписаны сертификатом — при скачивании из интернета SmartScreen предупредит; локальная установка работает без предупреждений |
| Per-machine установка «для всех» | установщик | реализована (UAC через Setup), но автоматически прогнан только per-user цикл |
| Размер Setup.exe (~117 МБ) | установщик | self-contained рантайм дважды (Setup + приложение в MSI); план — framework-dependent Setup или нативный стуб |

## Технические ограничения текущей реализации

- Файлы больше 2 ГБ не открываются (лимит `FPDF_LoadMemDocument`).
- После сохранения история Undo очищается (документ переоткрывается из
  результата) — задокументированное поведение, не баг.
- Аннотация, помеченная к удалению в панели комментариев, исчезает из
  панели сразу, но НА СТРАНИЦЕ (экран, печать, экспорт в изображения)
  остаётся видимой до сохранения: правки применяются к файлу при
  сохранении, как и весь остальной новый контент.
- Сетка режима «Страницы» не виртуализирована: на документах в тысячи страниц
  переключение в режим систематизации заметно дороже просмотра.
- Смена языка интерфейса применяется после перезапуска.
- Пароль к документу движок принимает в однобайтовой кодировке (латиница —
  надёжно; кириллические пароли зависят от того, чем файл шифровался).
- Тёмная тема не перекрашивает системные скроллбары и стандартные диалоги
  Windows (открытие/сохранение файлов).
- UI-автоматизация не выполнялась; функционально проверены движки (тестами),
  запуск/открытие (смоук по логу) и полный цикл установщика (per-user).
- Печать реализована, но не покрыта автотестами (нет надёжного способа
  печатать в файл без диалога драйвера); проверяйте на реальном принтере.
- Рендер выполняется в DIP-разрешении с учётом системного DPI на момент
  показа страницы; при переносе окна на монитор с другим DPI страница
  дорисуется в новом разрешении только после повторного попадания в
  виртуализацию (прокрутки).
